[Unit]
Description=SIF Website - Semantic Infrastructure Foundation Public Site
Documentation=https://github.com/semantic-infrastructure-lab/sif-website
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=scottsen
Group=scottsen
WorkingDirectory=/home/scottsen/src/projects/sif-website

# Pull latest image before starting
ExecStartPre=-/usr/bin/podman pull registry.mytia.net/sif-website:latest

# Run container
ExecStart=/usr/bin/podman run \
  --rm \
  --name sif-website \
  -p 0.0.0.0:8011:8000 \
  --health-cmd="curl -f http://localhost:8000/health || exit 1" \
  --health-interval=30s \
  --health-timeout=5s \
  --health-retries=3 \
  registry.mytia.net/sif-website:latest

# Stop container gracefully
ExecStop=/usr/bin/podman stop -t 10 sif-website

# Clean up container
ExecStopPost=-/usr/bin/podman rm -f sif-website

# Restart policy
Restart=on-failure
RestartSec=10s
StartLimitBurst=3
StartLimitIntervalSec=60s

# Resource limits
MemoryMax=512M
MemoryHigh=384M
CPUQuota=50%

# Security
NoNewPrivileges=true
PrivateTmp=true

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=sif-website

[Install]
WantedBy=multi-user.target
